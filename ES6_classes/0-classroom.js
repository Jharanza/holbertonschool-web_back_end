/**
 * Implement a class with an attribute
 * @maxStudentsSize attribute type number
 * @_maxStudentsSize variable that contain the value from the attribute
 */

export default class ClassRoom {
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
