/**
 * function that returns a promise
 * @p const that get the new promise
 * @export the function
 * @returns the nuew promise
 */

export default function getResponseFromAPI() {
  const p = new Promise((resolve) => resolve);

  return p;
}
