#!/usr/bin/env python3
""" Module that takes 2 strings, concatenate it and returns a string """


def concat(str1: str, str2: str) -> str:
    ''' Method that take 2 strings and concatenate it '''
    return str1 + str2
